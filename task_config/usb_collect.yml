save_dir: ./data

decimation: 1

save_frequency: 2
video_frequency: 2
render_frequency: 0  # 0 = headless (no GUI window)

random_texture: false

use_seed: true
episode_num: 100
start_seed: 0
max_seed: -1  # unlimited seeds until 100 successes collected

sensor_type: gsmini

observations:
  camera: ['rgb']
  tactile: ['rgb', 'rgb_marker', 'marker', 'depth', 'pose']
  embodiment: ['joint', 'ee']
  actor: true
